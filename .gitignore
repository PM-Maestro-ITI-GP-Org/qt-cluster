build/
build_*/
build-qnx/

# Rendered colour previews. Regenerate with tools/render_options.sh — the
# configs in options/*.json are what actually define each scheme.
options/*.png

# IDE session state
.vscode/
.qtcreator/*.user
.qtcreator/*.user.*
